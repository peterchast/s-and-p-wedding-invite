<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="https://emojicdn.elk.sh/🍻" />
    <title>{{ title or metadata.titleorig }}</title>
    <meta name="description" content="{{ description or metadata.descriptionorig }}">
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}">
  </head>
  <body>

      {{ content | safe }}

  </body>
</html>
